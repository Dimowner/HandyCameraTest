<?xml version="1.0" encoding="utf-8"?>
<manifest package="test.example.handycamera"
			 xmlns:android="http://schemas.android.com/apk/res/android">

	<uses-feature android:name="android.hardware.camera"/>
	<uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>

	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
	<uses-permission android:name="android.permission.CAMERA"/>

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_handy_camera"
		android:label="@string/app_name"
		android:supportsRtl="true"
		android:theme="@style/AppTheme">
		<activity
			android:name=".gallery.GalleryActivity"
			android:label="@string/app_name"
			android:theme="@style/AppTheme.NoActionBar">
			<intent-filter>
				<action android:name="android.intent.action.MAIN"/>

				<category android:name="android.intent.category.LAUNCHER"/>
			</intent-filter>
		</activity>

		<!--TODO: fix string-->
		<activity android:name=".camera.CameraActivity" android:label="Camera Activity"/>
		<activity android:name=".ImageEditActivity" android:label="Image edit Activity"/>
		<activity android:name=".ImagePreviewActivity" android:label="Image preview Activity"/>
	</application>

</manifest>
